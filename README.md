# archlinux-makepkg-docker
Docker image for ArchLinux package build testing
Personal AUR Devel Container

`docker build -t bionade24/aur-devel:local https://github.com/bionade24/archlinux-makepkg-docker.git`
